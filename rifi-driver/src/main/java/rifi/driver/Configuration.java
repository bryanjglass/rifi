package rifi.driver;

import org.springframework.context.annotation.Profile;

@Profile("driver")
public class Configuration {
    // This configuration is only active when the "driver" profile is activated
}
